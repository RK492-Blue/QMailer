import React from 'react';

const Landing = () => {
    return(
        <div style = {{ textAlign: 'center' }}>
            <h1>
                Welcome to Q-Mailer
            </h1>
            A Survey Web Application
        </div>
    );
};

export default Landing;